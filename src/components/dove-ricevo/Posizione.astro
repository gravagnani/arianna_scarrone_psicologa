---
// No props for now
import { CONTACTS } from "../../config/contacts.ts";
---

<section id="posizione">
  <div class="posizione-content">
    <div class="posizione-text">
      <h2>Come raggiungere lo studio</h2>
      <p>Lo studio si trova in Via L. Pellizzo 39, Padova. È raggiungibile:</p>
      <ul>
        <li>
          <strong>In auto:</strong> Diversi parcheggi gratuiti disponibili nelle vicinanze
        </li>
        <li>
          <strong>In bus:</strong> Linea U07, E071, E073, fermate “Ariosto” e “Padova
          V. Ariosto” a due minuti dallo studio
        </li>
        <li>
          <strong>In treno:</strong> Dalla stazione di Padova raggiungibile in 10
          minuti con l’autobus U07
        </li>
      </ul>
      <p>
        Per qualsiasi dubbio o necessità, non esitare a contattarmi prima della
        seduta.
      </p>
    </div>
    <div class="posizione-map">
      <iframe
        src={`https://www.google.com/maps?q=${encodeURIComponent(CONTACTS.address)}&output=embed`}
        width="100%"
        height="350"
        style="border:0; border-radius: 1rem;"
        allowfullscreen=""
        loading="lazy"
        referrerpolicy="no-referrer-when-downgrade"
        title="Mappa studio psicologico"></iframe>
    </div>
  </div>
</section>

<style>
  #posizione {
    width: 100%;
    padding: 3rem 2rem;
    background: #fff;
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  .posizione-content {
    display: flex;
    flex-direction: column;
    padding: 1.2rem 0.7rem;
    gap: 2.5rem;
    max-width: 1100px;
    width: 100%;
    align-items: flex-start;
  }
  .posizione-text {
    flex: 2;
    font-size: 1.13rem;
    color: var(--color-text-main, #222);
  }
  .posizione-text h2 {
    font-size: 1.5rem;
    color: var(--color-primary-dark, #6f8f7c);
    margin-bottom: 1rem;
  }
  .posizione-text ul {
    margin: 1rem 0 1.5rem 1.2rem;
    padding: 0;
    list-style: disc inside;
  }
  .posizione-text li {
    margin-bottom: 0.5rem;
  }
  .posizione-map {
    flex: 1;
    min-width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 1rem;
    box-shadow: -10px 10px 20px 0px rgba(0, 0, 0, 0.5);
  }
  .posizione-map iframe {
    min-height: 350px;
  }
  @media (max-width: 900px) {
    #posizione {
      padding: var(--space-xl) 0.7rem;
    }
    .posizione-content {
      flex-direction: column;
      gap: 2rem;
      align-items: center;
    }
    .posizione-map {
      max-width: 100%;
      width: 100%;
    }
  }
</style>
