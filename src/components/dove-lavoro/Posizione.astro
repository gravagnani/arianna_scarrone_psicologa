---
// No props for now
import { CONTACTS } from "../../config/contacts.ts";
---

<section class="posizione-section">
  <div class="posizione-content">
    <div class="posizione-text">
      <h2>Come raggiungere lo studio</h2>
      <p>
        Lo studio si trova in Via Esempio 123, 10100 Torino. È facilmente
        raggiungibile sia con i mezzi pubblici che in auto:
      </p>
      <ul>
        <li>
          <strong>In auto:</strong> Parcheggio disponibile nelle vicinanze.
        </li>
        <li>
          <strong>In autobus:</strong> Linee 11, 15, 55 fermata "Esempio" a pochi
          passi dallo studio.
        </li>
        <li>
          <strong>In treno:</strong> A circa 10 minuti dalla stazione Porta Susa.
        </li>
      </ul>
      <p>
        Per qualsiasi dubbio o necessità, non esitare a contattarmi prima della
        seduta.
      </p>
    </div>
    <div class="posizione-map">
      <iframe
        src={`https://www.google.com/maps?q=${encodeURIComponent(CONTACTS.address)}&output=embed`}
        width="100%"
        height="350"
        style="border:0; border-radius: 1rem;"
        allowfullscreen=""
        loading="lazy"
        referrerpolicy="no-referrer-when-downgrade"
        title="Mappa studio psicologico"></iframe>
    </div>
  </div>
</section>

<style>
  .posizione-section {
    width: 100%;
    padding: 3rem 2rem;
    background: #fff;
    display: flex;
    flex-direction: column;
    align-items: center;
  }
  .posizione-content {
    display: flex;
    flex-direction: column;
    gap: 2.5rem;
    max-width: 1100px;
    width: 100%;
    align-items: flex-start;
  }
  .posizione-text {
    flex: 2;
    font-size: 1.13rem;
    color: var(--color-text-main, #222);
  }
  .posizione-text h2 {
    font-size: 1.5rem;
    color: var(--color-primary-dark, #6f8f7c);
    margin-bottom: 1rem;
  }
  .posizione-text ul {
    margin: 1rem 0 1.5rem 1.2rem;
    padding: 0;
    list-style: disc inside;
  }
  .posizione-text li {
    margin-bottom: 0.5rem;
  }
  .posizione-map {
    flex: 1;
    min-width: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: 1rem;
    box-shadow: -10px 10px 20px 0px rgba(0, 0, 0, 0.5);
  }
  .posizione-map iframe {
    min-height: 350px;
  }
  @media (max-width: 900px) {
    .posizione-section {
      padding: 2rem 0.7rem;
    }
    .posizione-content {
      flex-direction: column;
      gap: 2rem;
      align-items: center;
    }
    .posizione-map {
      max-width: 100%;
      width: 100%;
    }
  }
</style>
