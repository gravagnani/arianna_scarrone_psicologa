---
import Logo from "../../assets/logos/logo_text_dark.svg";
import { Phone, Mail } from "@lucide/astro";
import { PATHS } from "../../config/paths.ts";
import { CONTACTS } from "../../config/contacts.ts";

const navbarContacts = [
  { logo: Phone, text: CONTACTS.phone },
  { logo: Mail, text: CONTACTS.email },
];
---

<header id="navbar">
  <!-- TOP CONTACT BAR -->
  <div id="contacts">
    {
      navbarContacts.map((contact) => (
        <div class="contact">
          <contact.logo class="icon" />
          <span>{contact.text}</span>
        </div>
      ))
    }
  </div>

  <!-- MAIN NAV -->
  <nav id="links" aria-label="Main navigation">
    <a href="/"><Logo class="logo" /></a>

    <button id="menu-toggle" aria-label="Toggle menu" type="button">
      <span></span>
      <span></span>
      <span></span>
    </button>

    <div id="nav-menu">
      {PATHS.map((link) => <a href={link.href}>{link.text}</a>)}

      <!-- MOBILE CONTACTS -->
      <div id="contacts-mobile">
        {
          navbarContacts.map((contact) => (
            <div class="contact">
              <contact.logo class="icon" />
              <span>{contact.text}</span>
            </div>
          ))
        }
      </div>
    </div>
  </nav>
</header>

<script type="text/javascript">
  document.addEventListener("DOMContentLoaded", function () {
    const menuToggle = document.getElementById("menu-toggle");
    const navMenu = document.getElementById("nav-menu");
    menuToggle?.addEventListener("click", () => {
      navMenu?.classList.toggle("active");
      menuToggle?.classList.toggle("active");
    });
    // Close menu when clicking on a link
    navMenu?.querySelectorAll("a")?.forEach((link) => {
      link.addEventListener("click", () => {
        navMenu?.classList.remove("active");
        menuToggle?.classList.remove("active");
      });
    });
  });
</script>

<style>
  #navbar {
    background: var(--color-primary, #6f8f7c);
  }

  #contacts {
    padding: var(--space-xs) var(--space-xxl);
    display: flex;
    flex-direction: row;
    width: 100%;
    height: auto;
    gap: var(--gap-md);
    border-bottom: 1px solid var(--color-primary-dark, #5a7266);
  }

  #contacts-mobile {
    display: none;
  }

  #links {
    padding: var(--space-sm) var(--space-md);
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    gap: 20px;
    position: relative;
  }

  #nav-menu {
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: 20px;
  }

  #menu-toggle {
    display: none;
    flex-direction: column;
    background: none;
    border: none;
    cursor: pointer;
    padding: 5px;
    gap: 4px;
    position: absolute;
    right: var(--space-md);
    z-index: 1001;
  }

  #menu-toggle span {
    width: 25px;
    height: 3px;
    background-color: var(--color-text-main);
    transition: all 0.3s ease;
    border-radius: 2px;
  }

  #menu-toggle.active span:nth-child(1) {
    transform: rotate(45deg) translate(5px, 5px);
  }

  #menu-toggle.active span:nth-child(2) {
    opacity: 0;
  }

  #menu-toggle.active span:nth-child(3) {
    transform: rotate(-45deg) translate(7px, -6px);
  }

  .contact {
    display: flex;
    align-items: center;
    gap: 5px;
    font-size: 14px;
    color: var(--color-text-secondary);
  }

  nav a {
    text-decoration: none;
    font-weight: bold;
    color: var(--color-text-main);
  }

  nav a:hover {
    text-decoration: underline;
  }

  .icon {
    width: 16px;
    height: 16px;
  }

  .logo {
    height: 120px;
    width: auto;
  }

  /* Mobile Responsive */
  @media (max-width: 768px) {
    #contacts-mobile {
      display: flex;
      flex-direction: column;
      gap: var(--gap-sm);
      width: 100%;
      margin-top: 20px;
      border-top: 1px solid var(--color-primary-dark);
      padding-top: 10px;
    }
    #contacts {
      display: none;
    }

    #links {
      justify-content: space-between;
      padding: var(--space-sm);
    }

    .logo {
      height: 80px;
    }

    #menu-toggle {
      display: flex;
    }

    #nav-menu {
      position: fixed;
      top: 0;
      right: -100%;
      width: 70%;
      max-width: 300px;
      height: 100vh;
      background: var(--color-primary);
      flex-direction: column;
      align-items: flex-start;
      padding: 80px 30px 30px;
      gap: 30px;
      box-shadow: -14px 0px 20px 10px rgba(0, 0, 0, 0.5);
      transition: right 0.3s ease;
      z-index: 1000;
      overflow-y: auto;
    }

    #nav-menu.active {
      right: 0;
    }

    #nav-menu a {
      width: 100%;
      padding: 10px 0;
      font-size: 18px;
    }
  }

  @media (max-width: 480px) {
    .logo {
      height: 80px;
    }

    #nav-menu {
      width: 80%;
    }
  }
</style>
