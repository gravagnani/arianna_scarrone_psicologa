---
// No props for now
import { Phone, Mail } from "@lucide/astro";
import WhatsAppLogo from "../../assets/icons/whatsapp.svg";
import { CONTACTS } from "../../config/contacts.ts";
---

<section id="chiamami">
  <div class="chiamami-content">
    <h2>Hai bisogno di aiuto o vuoi maggiori informazioni?</h2>
    <p>
      Non esitare a contattarmi: sono qui per ascoltarti e offrirti il supporto
      di cui hai bisogno. Puoi chiamarmi direttamente per fissare un
      appuntamento o per ricevere una prima consulenza telefonica gratuita.
    </p>
    <div class="chiamami-buttons">
      <a href={`tel:${CONTACTS.phone}`} class="chiamami-btn">
        <Phone class="button-icon" /> Chiamami
      </a>
      <a href={`mailto:${CONTACTS.email}`} class="chiamami-btn">
        <Mail class="button-icon" /> Scrivimi una email
      </a>
      <a href={`https://wa.me/${CONTACTS.phone}`} class="chiamami-btn">
        <img class="button-icon" src={WhatsAppLogo.src} alt="WhatsApp" /> Scrivimi
        su WhatsApp
      </a>
    </div>
  </div>
</section>

<style>
  #chiamami {
    width: 100%;
    padding: 3rem 1.5rem;
    background: var(--color-primary-light, #c6d6cc);
    display: flex;
    justify-content: center;
  }
  .chiamami-content {
    max-width: 600px;
    width: 100%;
    text-align: center;
    background: #fff;
    padding: 2rem 1.5rem;
    border-radius: 1rem;
    box-shadow: 0 2px 12px rgba(0, 0, 0, 0.06);
  }
  .chiamami-content h2 {
    font-size: 2rem;
    color: var(--color-primary-dark, #6f8f7c);
    margin-bottom: 1rem;
  }
  .chiamami-content p {
    font-size: 1.15rem;
    color: var(--color-text-main, #222);
    margin-bottom: 2rem;
  }
  .chiamami-buttons {
    display: grid;
    justify-content: center;
    gap: 1rem;
  }
  .chiamami-btn {
    display: inline-flex;
    align-items: center;
    padding: 0.85rem 2.2rem;
    font-size: 1.13rem;
    font-weight: 600;
    color: #fff;
    background: var(--color-primary, #94b09f);
    border: none;
    border-radius: 0.5rem;
    cursor: pointer;
    transition: background 0.2s;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.07);
    text-decoration: none;
  }
  .chiamami-btn:hover {
    background: var(--color-primary-dark, #6f8f7c);
  }

  .button-icon {
    display: inline-block;
    vertical-align: middle;
    margin-right: 0.7rem;
    width: 30px;
    height: 30px;
  }

  @media (max-width: 600px) {
    .chiamami-content {
      padding: 1.2rem 0.5rem;
    }
    #chiamami {
      padding: 2rem 0.7rem;
    }
  }
</style>
